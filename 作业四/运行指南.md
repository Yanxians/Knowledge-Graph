# 作业四运行指南
## 数据导入  
* Neo4j：  
在data里装载了可以直接导入neo4j的数据集。只需找到neo4j配置下的import包，将数据导入其中即可。  
之后在neo4j内，运行import内的代码即可。
* TuGraph：
首先运行build_schema.py，在TuGraph中建立schema。  
删除data里各个csv的最后一列，进入TuGraph的图形化界面导入csv文件。
## 问答知识系统
llm_answer.py里有两个函数neo4j_answer()和Tugraph_answer()。修改对应的参数后，运行函数即可。
